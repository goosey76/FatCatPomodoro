import Foundation

// Music add-on excluded — stub kept so the type name remains available if referenced elsewhere.
class MusicManager: ObservableObject {
    static let shared = MusicManager()
    private init() {}
}
